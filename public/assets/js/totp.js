let totp_inputs=[];for(let i=1;i<=6;i++){totp_inputs[i]=document.getElementById(`totp${i}`);totp_inputs[i].addEventListener("input",e=>{if(e.inputType!=="insertText")return;let next_input=totp_inputs[i+1];next_input&&next_input.focus()});totp_inputs[i].addEventListener("keydown",e=>{if(e.key==="Backspace"){if(e.target.value===""){let new_input=totp_inputs[i-1];if(new_input){new_input.focus();new_input.value=""}}}})}totp_inputs[1].addEventListener("paste",e=>{let clipboard=e.clipboardData||window.clipboardData;let clipboard_data=clipboard.getData("Text");let upper_bound=clipboard_data.length<=6?clipboard_data.length:6;for(let i=0;i<upper_bound;i++){totp_inputs[i+1].value=clipboard_data[i]}totp_inputs[upper_bound].focus()});
